// cach mo shadow cho root - nó là cái bóng của DOM dùng xây dựng các app dựa trên các component
constructor(){
  supper()
  this.attactShadow({mode: 'open'}) //đây là cách mở khai báo shadow
}

this.shadowRoot.innerHTML= ` `; //đây là cách dùng

//-------------------------------------------------------------------------
array.prototype.includes(index) //trả về giá trị true - false

//-------------------------------------------------------------------------
array.slice(index) //trích xuất phần tử trong mảng

//-------------------------------------------------------------------------
array.indexOf(item, start)

// Trong đó:

// item là giá trị của phần tử cần tìm.
// start là vị trí bắt đầu tìm kiếm.
//khhi không tìm thấy trả về giá trị -1
